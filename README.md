# Network Traffic Generation by Large Language Models (LLMs)

This repository presents a novel approach for generating **realistic network traffic** using **Large Language Models (LLMs)**, specifically **OpenAI’s GPT-4.1** and **GPT-5**.  

Our method, called the **Large Language Model Network Traffic Generator (LLM-NTG)**, aims to bridge the gap between **realistic traffic generation** and the **expressive capabilities of LLMs**.  
We employ a few-shot learning framework combined with a **human-in-the-loop feedback mechanism**, where generated traffic is continuously evaluated and refined.

Moreover, repository included baseline models based Recurrent Neural Network (RNN) and Generative Adversarial Networks (GAN) deep learning models.

---

## 📂 Repository Structure

### 1. `Datasets/`
Contains datasets required for traffic generation:
- **One-way** and **two-way traffic datasets**.
- **Sample inputs** in `.json`, `.pcapng`, and `.csv` formats for traffic generation.

---

### 2. `GPT-4.1/`
Experiments performed with **GPT-4.1**.  
This folder has two subfolders: `Experiment_1/` and `Experiment_2/`.

Each experiment includes:
- **`Exp1_Sample_Packets_Extraction.ipynb`**  
  Extracts traffic data from the dataset and prepares **sample packets** for generation.
- **`Exp1_Traffic_Generation_gpt4.1.ipynb`**  
  Generates synthetic traffic and saves the output as `.json` files.
- **`Exp1_Statistics_of_Generated_Traffics_gpt4.1.ipynb`**  
  Computes statistics of the generated traffic.

(`Experiment_2/` follows the same structure.)

---

### 3. `GPT-5/`
Experiments performed with **GPT-5**.  
This folder also has `Experiment_1/` and `Experiment_2/`.

Since the **same input samples** are used as in GPT-4.1, there is no extraction notebook here.  
Each experiment includes:
- **`Exp1_Traffic_Generation_gpt5.ipynb`**  
  Generates synthetic traffic with GPT-5.
- **`Exp1_Statistics_of_Generated_Traffics_gpt5.ipynb`**  
  Computes statistics of the generated traffic.

(`Experiment_2/` follows the same structure.)

---

### 4. `GAN/`
Experiments performed with **GAN**.  

Since the **same input samples** are used as in GPT-4.1, there is no extraction notebook here.  
- **`gan_generator.py`**  
  Generates synthetic traffic with GAN.
- **`Statistics_GAN.ipynb`**  
  Computes statistics of the generated traffic.

---

  ### 5. `RNN/`
Experiments performed with **RNN**.  

- **`EXP1_Traffic_Generation_rnn.ipynb`**  
  Implements the RNN model for **Experiment 1**, focusing on unidirectional device-to-hub ZigBee traffic generation.

- **`EXP1_Traffic_Generation_30minute.ipynb`**  
  Extends Experiment 1 to evaluate the **generalization ability** of the RNN by generating **30 minutes of synthetic traffic**.

- **`EXP2_Traffic_Generation_rnn.ipynb`**  
  Implements the RNN model for **Experiment 2**, which captures **bidirectional request–response communication patterns**.

- **`EXP2_Traffic_Generation_30minute.ipynb`**  
  Evaluates long-duration traffic generation for Experiment 2, assessing temporal stability and protocol consistency over extended sequences.

- **`Evaluation_RNN.ipynb`**  
  Computes all evaluation metrics for RNN-generated traffic, including:
  - Decodability
  - Protocol compliance
  - Address compliance
  - Repetition rate
  - Exact Match Rate (EMR)
  - Sequence ordering correctness

- **`Statistics_RNN.ipynb`**  
  Performs statistical analysis and aggregation of results across multiple runs, enabling comparison with LLM-based, GAN, and other baseline models.

---

### 6. `Generated_Traffic/`
Contains the **generated traffic outputs** in `.json` format and .pcap files.  
- Each experiment’s results are stored here.
- The `JSON_files/` subfolder contains details such as: .json format of generated traffic
- The `PCAP_files/` subfolder contains details such as: There are .pcap files of the generated traffi,c and Wireshark can be used to view them.
- (OLD RESULTS) The `Results/` subfolder contains details such as:
  - **Token usage**
  - **Computation time**
- The `New_Experiments/` subfolder contains of generated network traffic packets from GPT 5, GPT 4.1 and packets generated to test generalization ability (30 minutes packets) of all models.
- The `TXT_files/` subfolder contains generated RAW packets by RNN.

---

### 7. `pcap_converter.py`   
A transformation script that converts generated `.json` traffic files into **`.pcap` format**, enabling further analysis with standard network traffic tools (e.g., Wireshark).

---

### 8. PCAP_GPT4.1_Experiment_1.png and  PCAP_GPT5_Experiment_1.png   
Wireshark representation of traffic generated by GPT 4.1 and 5 for Experiment~1.

---

### 🧪 Generalization Ability (Long-Duration Traffic)

The following notebooks evaluate the ability of LLMs to **generalize beyond the prompt samples** by generating **30 minutes of continuous synthetic ZigBee traffic**.  
These experiments assess temporal stability, protocol compliance, and scalability under extended generation horizons.

- **Experiment 1 – Unidirectional Traffic**
  - `EXP1_GPT41_Generalization_Ability_30minute.ipynb`
  - `EXP1_GPT5_Generalization_Ability_30minute.ipynb`

- **Experiment 2 – Bidirectional Request–Response Traffic**
  - `EXP2_GPT41_Generalization_Ability_30minute.ipynb`
  - `EXP2_GPT5_Generalization_Ability_30minute.ipynb`

---

### 🧠 Reasoning Effort Analysis

These notebooks analyze the impact of **model reasoning configurations** on traffic generation quality.  
Different reasoning levels are explored to understand trade-offs between **structural correctness**, **semantic consistency**, and **computational cost**.

- **Experiment 1 – Reasoning Effort**
  - `EXP1_New_GPT41_Reasoning_Effort.ipynb`
  - `EXP1_New_GPT5_Reasoning_Effort.ipynb`
  - `EXP1_New_GPT5_Reasoning_Effort_high.ipynb`
  - `EXP1_New_GPT5_Reasoning_Effort_low.ipynb`

- **Experiment 2 – Reasoning Effort**
  - `EXP2_New_GPT41_Reasoning_Effort.ipynb`
  - `EXP2_New_GPT5_Reasoning_Effort.ipynb`
  - `EXP2_New_GPT5_Reasoning_Effort_high.ipynb`
  - `EXP2_New_GPT5_Reasoning_Effort_low.ipynb`

---

### 📊 Supporting Artifacts

- **PCAP Visualizations**
  - `PCAP_GPT4.1_Experiment_1.png`
  - `PCAP_GPT5_Experiment_1.png`  
  These figures provide packet-level visual inspection of generated traffic.

- **Comprehensive Analysis Report**
  - `PDF_Analysis_Generated_Traffic_ALL_MODELS.pdf`  
  A consolidated report summarizing evaluation metrics and comparisons across **LLMs, RNN, and GAN baselines**.

---

## 🚀 Usage
1. Explore the **datasets** under `Datasets/`.  
2. Run the notebooks in `GPT-4.1/` or `GPT-5/` for traffic generation.  
3. Generated traffic will be saved under `Generated_Traffic/`.  
4. Optionally, convert `.json` traffic files into `.pcap` format using:
   ```bash
   python pcap_converter.py input.json output.pcap
